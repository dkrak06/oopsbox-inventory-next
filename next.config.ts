/**
 * 이 파일은 Next.js 애플리케이션의 기본 환경 설정을 담당합니다.
 * 이미지 외부 도메인 및 번들러 설정을 정의하여 애플리케이션이 안정적으로 동작하게 돕습니다.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
